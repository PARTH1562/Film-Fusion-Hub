## Packages
(none needed)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
All required packages (framer-motion, react-hook-form, zod, lucide-react, etc.) are already in the base Replit stack.
Defaulting to a dark cinematic theme using CSS variables.
